interface IProcessEnvironment {
    [key: string]: string | undefined;
}
type PlatformName = 'Web' | 'Windows' | 'Mac' | 'Linux';
declare const enum OperatingSystem {
    Windows = 1,
    Macintosh = 2,
    Linux = 3
}

export { type IProcessEnvironment, OperatingSystem, type PlatformName };
