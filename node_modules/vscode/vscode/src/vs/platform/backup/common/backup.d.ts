import { URI } from '../../../base/common/uri.js';
import { IWorkspaceIdentifier } from '../../workspace/common/workspace.js';

interface IBaseBackupInfo {
    remoteAuthority?: string;
}
interface IWorkspaceBackupInfo extends IBaseBackupInfo {
    readonly workspace: IWorkspaceIdentifier;
}
interface IFolderBackupInfo extends IBaseBackupInfo {
    readonly folderUri: URI;
}

export type { IBaseBackupInfo, IFolderBackupInfo, IWorkspaceBackupInfo };
