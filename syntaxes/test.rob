fun Number procedure {
    If(false){
        then{
            Clock(90);
            setSpeed(90,MM);
            Number Distance = getDistanceFromCaptor(CM);
            Number Distance = 2;
        }
        else{
            Clock(90);
            }
    };
    Repeat(false){
     Deplacement(AVANT, 1, CM);
     Repeat(false){
        setSpeed(90, MM);
    };
    };

    Clock(90);
    Number Distance = getDistanceFromCaptor(CM);
    Number Melvin = (8 + 4);
    Number Enzo = (Melvin * 10);
}

fun Void test{}