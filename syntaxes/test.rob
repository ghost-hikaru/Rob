fun Void square(){
  Deplacement(AVANT, 150, CM);
  Clock(9000);
  Deplacement(GAUCHE, 150, CM);
  Clock(9000);
  Deplacement(DROITE, 150, CM);
  Clock(9000);
  Deplacement(ARRIERE, 150, CM);
  Clock(9000);
}

fun Void mainCode() {
  Number count = 0;
  Repeat((count < 5)){
    count = (count + 1);
    square();
  };
}
